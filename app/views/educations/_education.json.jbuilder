json.extract! education, :id, :name, :number, :email, :about, :accepted_documents, :services, :status, :latitude, :longitude, :created_at, :updated_at
json.url education_url(education, format: :json)
