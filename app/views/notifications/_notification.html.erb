<div class="panel">
  <div class="panel-body">
    <strong><%=link_to notification.content, conversation_messages_path(notification.message.conversation_id)%></strong>
    <span class="pull-right"><%= notification.created_at.to_formatted_s(:short) %></span>
  </div>
</div>
