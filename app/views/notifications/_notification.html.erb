<div class="panel">
  <div class="panel-body">
  <%# <%sender_id = Conversation.where(recipient_id: notification.user_id)[0].sender_id%> 
  <%# <%recipien_id = Conversation.where(sender_id: notification.user_id).reverse.first.recipient_id%>
    <%# <%byebug%> 
    <strong><%=link_to notification.content, conversation_messages_path(notification.message.conversation_id)%></strong>
    <span class="pull-right"><%= notification.created_at.to_formatted_s(:short) %></span>
  </div>
</div>
